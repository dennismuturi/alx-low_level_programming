#include "main.h"
/**
 * _puts - a function that prints a string, followed by a new line
 * @str: a string
 */
void _puts(char *str)
{
	while (*s != '\0')
	{
		_putchar(*s);
		s++;
	}
	_putchar('\n');
}
